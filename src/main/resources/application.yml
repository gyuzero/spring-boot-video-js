spring:
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
  h2:
    console:
      path: /h2-console
      enabled: true
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB

mybatis:
  mapper-locations: classpath:mapper/*.xml

file:
  store:
    path: /Users/gyuyoung/upload